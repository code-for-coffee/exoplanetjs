var app = app || {};

app.conversions = app.conversions || {};

app.conversions.parseJupiterMassFromNASAPlanetData = function(exoplanetObject) {
  if (!typeof exoplanet == 'object') return false;

  var exoplanetMass;

  return exoplanetMass;

};
