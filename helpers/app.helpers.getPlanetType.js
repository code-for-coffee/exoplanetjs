var app = app || {};

app.helpers = app.helpers || {};

/**
 *  @earthMass: Float value for a planet's relative Earth Mass
 *  return String of Official Planet Type
 */
app.helpers.getPlanetType = function(earthMass) {

  var planetType;

  return planetType;

};
